/**
 * Shared type definitions for MK Studio.
 *
 * Types used by both client and server live here.
 * Currently re-exports the database types for convenience.
 */

export type { Database } from "../../client/src/types/database";
